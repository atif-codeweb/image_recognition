A deep learning project that uses Convolutional Neural Networks (CNN) to classify images as either cats or dogs.
This project implements a CNN using TensorFlow and Keras to distinguish between cat and dog images with high accuracy.
The model learns hierarchical features from images through multiple convolutional and dense layers.
